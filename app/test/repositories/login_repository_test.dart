import 'package:app/bloc/cubit/login_cubit.dart';
import 'package:app/config/api_client.dart';
import 'package:app/repositories/login_repository.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Realizar login', () async {
    // final api = HttpApiClient();
    // final res = await LoginRepository(api: api).authentication('admin@email.com.br', 'Teste@123');

    // expect(res.isRight, true);
    // expect(res.right, isA<LoginSuccess>());
  });
}